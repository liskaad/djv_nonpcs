Config = {}

-- Enable or disable the script
Config.Enabled = true

-- Remove scenario peds (random peds walking around)
Config.RemoveScenarioPeds = true

-- Disable vehicle generators (parked cars with NPCs)
Config.DisableVehicleGenerators = true

-- Density multipliers (0.0 = no spawns, 1.0 = normal)
Config.PedDensity = 0.0
Config.VehicleDensity = 0.0

-- Remove specific scenario types
Config.RemoveScenarios = true
